---
description: End the current English training session and save its complete history and resume position.
agent: build
---

Load and follow the `end-training` skill now.

Archive all available content from the current English training session, including every prompt, learner answer, retry, correction, accepted sentence, and provided Chinese translation. Then update the canonical progress record and report the saved paths and exact resume position.

Work only inside the current workspace. Do not read or write global OpenCode configuration. Do not continue the drill or issue a new exercise prompt after saving.
